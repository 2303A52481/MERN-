/*
// console.log(document.getElementById("heading"))
heading = document.getElementById("heading").style.color = "blue"
// heading.style.color = "blue"
// heading.style.backgroundColor = "green"

const box = document.getElementsByClassName("box")
// box.style.color="red"
box.forEach((b) => {
    b.style.color = "red";
    b.style.backgroundColor = "lightblue";
});

const box=document.getElementsByClassName("box")
for(let i=0;i<box.length;i++){

}
const arr=Array.from(box)
console.log(box)
arr.forEach((division)=>{division.style.color="red"})

const heading = document.getElementsByTagName('h2')
console.log(heading)
for(let i=0; i<=heading.length;i++){
    heading[i].style.color="green"
}
    

const heading = document.querySelector(".box") // will selects the single element starting element 
heading.style.color = "blue"

const box=document.querySelectorAll(".box") // will selects all the element with the class name box
console.log(box)
box.forEach((item)=>{
    item.style.color="red"
    //item.style.backgroundColor="lightblue"
})
    */
   